# @innei-util/prettier

## 简介

在个人项目中统一 prettier 配置，将之封装为 npm 包。

## 使用

安装依赖

```shell
yarn add -D @innei-util/prettier
```

在 `package.json` 中配置

```json
{
  "prettier": "@innei-util/prettier"
}
```
